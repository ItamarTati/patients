import React from 'react';
import classes from './Button.module.css';

const Button: React.FC = () => {
    return (
        <button className={classes.Button}>Go to Previous Page</button>
    )
  }


export default Button